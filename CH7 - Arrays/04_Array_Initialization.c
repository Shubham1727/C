#include <stdio.h>

int main()
{
    char a[] = {'z', 's', 'x'};
    printf("The value of a[1] is %c\n", a[1]);
    printf("The value of a[2] is %c\n", a[2]);
    printf("The value of a[0] is %c\n", a[0]);
    return 0;
}